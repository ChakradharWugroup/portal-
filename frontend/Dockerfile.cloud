# Use Node 18
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy all frontend files
COPY . .

# Expose Vite port
EXPOSE 3004

# Run Vite preview or dev
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3004"]
